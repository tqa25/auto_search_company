# Auto Search Company 프로젝트 핸드오버

> 작성일: 2026-07-29
> V1 비교용 동결: `/home/ubuntu/workspaces2/projects/auto_search_company_v1`
> 루트에서 V2 계속 개발: `/home/ubuntu/workspaces2/projects/auto_search_company`

## 1. 인계되는 프로젝트

Auto Search Company는 회사 목록을 받아 관련 URL을 찾고, 콘텐츠를 스크랩하며, AI를 사용하여 전화번호, 이메일, 주소, 웹사이트, 팩스 및 대표자를 추출합니다.

V1은 비교용 및 롤백 방안으로 유지됩니다. 프로젝트를 인계받는 팀은 현재 루트에서 V2 개발을 계속하며, V1 백업 디렉터리에서는 수정하지 않습니다.

이 문서는 시작 페이지입니다. 이 문서는 무엇을 인계받아야 하는지, 어떤 위험을 알아야 하는지, 그리고 다음으로 어떤 문서를 읽어야 하는지를 알려줍니다.

## 2. 인계받아야 할 파일 세트

Git 리포지토리에는 다음이 포함되어야 합니다:

- `src/`, `dashboard/`, `scripts/` 및 `tests/`;
- `requirements.txt`, `.env.example` 및 `pipeline_config.json`;
- `AGENTS.md`;
- `docs/v2-modular-refactor-plan.md`;
- `docs/v2-modular-refactor-plan.en.md`;
- `docs/v2-stage1-critical-fixes-implementation-plan.md`;
- `docs/v1-operational-audit.md`;
- 이 핸드오버 문서.

코드 패키지에 `.env`, API 키, 토큰, `venv/`, 로그, 출력 또는 임시 파일을 포함하지 마십시오.

데이터베이스와 `Factory PIC Collection progress.xlsx`는 `.gitignore`에 의해 무시되고 있습니다. 이들은 Git을 통해 자동으로 포함되지 않으며, 승인된 데이터 채널을 통해서만 전송되어야 합니다.

데이터베이스를 인계할 경우, 스냅샷을 생성하기 전에 워커를 중지해야 합니다. 기록에는 스냅샷 날짜, SHA-256 무결성 검사 코드, 주요 레코드 수 및 `PRAGMA integrity_check` 결과가 포함되어야 합니다.

## 3. V1의 가장 중요한 다섯 가지 경고

### 3.1 배치 AI가 소스 추적을 잘못 처리함

V1은 동일 회사에 속하는 여러 URL의 마크다운을 하나의 AI 호출로 묶었습니다. 앱은 이후 배치 내 모든 URL에 대해 동일한 응답 연락처를 저장했습니다.

데이터베이스는 5,964개 그룹에 속하는 22,696개의 연락처 레코드가 이 흔적을 가지고 있으며, 5,645개 회사에 영향을 미친다는 것을 확인했습니다.

스크랩된 콘텐츠는 이 오류로 인해 광범위하게 덮어쓰여지지 않았습니다. 신뢰할 수 없는 부분은 `extracted_contacts` 내의 "이 연락처가 어떤 URL에서 왔는지" 관계입니다.

V1 데이터를 사용할 때, 각 전화번호/이메일을 올바른 URL의 `scraped_pages.markdown_content`와 비교하여 확인해야 합니다.

### 3.2 `done` 상태가 연락처 존재를 보장하지 않음

데이터베이스에는 `done` 상태이지만 연락처가 없는 156개 회사가 있습니다.

`companies.status`만 사용하여 완료율을 계산하지 마십시오. 실제 데이터를 확인하고 완료됨, 미완료, 연락처 없음 및 실패를 명확히 구분해야 합니다.

### 3.3 데이터베이스에 중복 데이터가 있음

다음이 확인되었습니다:

- 89,070개의 중복 검색 레코드;
- 33,953개의 중복 필터링된 링크 레코드;
- 8,665개의 중복 스크랩된 페이지 레코드;
- 여러 연락처 레코드를 가진 696개의 스크랩된 페이지.

레코드 수는 특정 회사가 더 나은 데이터를 가지고 있음을 증명하지 않습니다.

### 3.4 Quick Search는 미확인 데이터임

12,072개의 `gemini_grounding` 연락처가 `scraped_page_id`가 연결되어 있지 않습니다.

Quick Search에 의해 추가된 베트남어 회사명, 세금 코드 및 연락처는 독립적인 출처가 확인될 때까지 미확인 데이터로 간주되어야 합니다.

### 3.5 병합(Merge)이 전체 회사 데이터를 대체할 수 있음

`scripts/merge_db.py`는 특정 회사의 모든 대상 데이터를 삭제한 다음 다른 데이터베이스의 버전을 복사할 수 있습니다.

`smart` 모드는 레코드 수를 사용하여 풍부도를 계산합니다. 중복 데이터 또는 배치 AI로 인해 품질이 낮은 버전이 승리할 수 있습니다.

백업, 드라이런(실제 변경 없는 사전 실행), 영향을 받는 회사 목록 및 승인자가 없을 경우 프로덕션 데이터베이스에서 병합을 실행하지 마십시오.

## 4. 아직 공식적인 클린 코드 마일스톤이 없음

이 문서를 작성할 때:

- V1 백업과 V2는 동일한 커밋 `8be26342...`에서 시작했습니다;
- V1 백업에는 많은 변경 사항이 있으며 Git이 추적하지 않는 파일이 있습니다;
- V2에는 수정, 파일 삭제 및 아직 커밋되지 않은 새 문서가 있습니다.

현재 커밋을 공식 핸드오버 버전으로 선언하지 마십시오.

제출하기 전에:

1. 전체 `git diff`를 검토하십시오.
2. 정리 변경 사항을 기능 변경 사항과 분리하십시오.
3. 적절한 테스트를 실행하십시오.
4. V1 동결 마일스톤을 커밋하십시오.
5. V2 시작 마일스톤을 커밋하십시오.
6. 두 마일스톤에 명확한 태그를 지정하십시오.
7. 별도 기록에 데이터베이스 체크섬을 기록하십시오.

## 5. 문서 맵

| 알아야 할 사항 | 문서 |
|---|---|
| 프로젝트 인계 및 파일 확인 | `PROJECT_HANDOVER.md` |
| V1 작동 방식, 신뢰할 수 없는 데이터 | `docs/v1-operational-audit.md` |
| V2가 구축해야 할 동작 및 아키텍처 | `docs/v2-modular-refactor-plan.md` |
| 엔지니어 및 AI 에이전트를 위한 V2 요약본 | `docs/v2-modular-refactor-plan.en.md` |
| 첫 세 가지 버그 수정 | `docs/v2-stage1-critical-fixes-implementation-plan.md` |
| AI 에이전트 필수 규칙 | `AGENTS.md` |

권장 읽기 순서:

1. `PROJECT_HANDOVER.md`.
2. `docs/v1-operational-audit.md`의 1, 4, 5항.
3. V2 계획의 1-3항 및 8항.
4. 코드 수정 전 Stage 1 계획.
5. 구현을 시작할 때 아키텍처, 테스트 및 에이전트 프로토콜 항목.

## 6. 핸드오버 확인 체크리스트

### 코드

- [ ] 워킹 트리가 검토되고 커밋되었습니다.
- [ ] V1 및 V2 마일스톤에 태그가 지정되었습니다.
- [ ] 핸드오버 커밋에서 `git status`가 깨끗합니다.
- [ ] 명령 및 테스트 결과가 있습니다.
- [ ] 패키지에 `.env` 또는 비밀 정보가 없습니다.
- [ ] 인계받는 사람이 다른 머신에서 클론하고 실행할 수 있습니다.

### 데이터베이스

- [ ] 스냅샷 전에 워커가 중지되었습니다.
- [ ] `PRAGMA integrity_check`가 `ok`를 반환합니다.
- [ ] SHA-256 코드 및 레코드 수가 있습니다.
- [ ] 인계받는 사람이 배치 AI 및 중복 데이터 경고를 읽었습니다.
- [ ] 찾을 수 있다면 병합/스냅샷 기록 목록이 있습니다.
- [ ] 데이터 접근 권한이 승인되었습니다.

### 문서 및 책임

- [ ] 인계받는 사람이 V2 계획이 향후 동작에 대한 표준 소스임을 확인했습니다.
- [ ] 미해결 질문이 채팅에만 남아있지 않고 작업 항목에 기록되었습니다.
- [ ] 첫 코드 수정 전에 진행 부트스트랩 세트가 생성되었습니다.
- [ ] 코드 세션 종료 전에 `scripts/check-doc-sync.sh`가 통과되었습니다.
- [ ] 프로덕션 데이터베이스에서 실행하기 전에 마이그레이션 승인자가 있습니다.

## 7. 현재 결론의 한계

누가 병합을 실행했는지, 언제 실행했는지, 또는 어떤 회사들이 대체되었는지는 아직 확인되지 않았습니다.

재추출 파일이 리포지토리 외부에 있는 도구를 사용하여 수동으로 임포트되었을 가능성을 배제할 수 없습니다.

공식적인 결론이 필요할 경우, 날짜별 백업, 셸 기록, CI 로그 및 전달된 내보내기 파일을 추가로 대조해야 합니다.
